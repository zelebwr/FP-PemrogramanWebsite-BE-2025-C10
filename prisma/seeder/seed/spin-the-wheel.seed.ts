// Spin The Wheel Seeder (Template Only)
// Note: Game Template is seeded via game-templates.data.csv
// This file is kept if we need specific imperative seeding logic for the template in the future.
// For now, it does nothing as the CSV handles the template creation.

export const spinTheWheelSeed = async () => {
  await Promise.resolve();
  // No-op: Template handled by CSV
  console.log(
    '🌱 Spin The Wheel template is handled by generic template seeder.',
  );
};
